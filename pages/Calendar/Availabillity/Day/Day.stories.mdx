import { Canvas, Story, Meta } from "@storybook/addon-docs";

import Day from "./Day";

<Meta title="Pages/Calendar/Availabillity/Day" component={Day} />

export const Template = (args) => <Day {...args} />;

# Day

The Day button cycles through the four availabillity props when clicked:
`undefined` (not available), `AVAILABLE`, `CLOCK`, and `WALK`

<Canvas>
  <Story name="Day" args={{ date: 12 }}>
    {Template.bind({})}
  </Story>
  <Story name="Available" args={{ date: 12, type: "AVAILABLE" }}>
    {Template.bind({})}
  </Story>
  <Story name="Clock" args={{ date: 12, type: "CLOCK" }}>
    {Template.bind({})}
  </Story>
  <Story name="Walk" args={{ date: 12, type: "WALK" }}>
    {Template.bind({})}
  </Story>
</Canvas>

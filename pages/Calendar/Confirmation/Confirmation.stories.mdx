import { Canvas, Story, Meta } from "@storybook/addon-docs";

import Confirmation from "./Confirmation";

<Meta title="Pages/Calendar/Confirmation" component={Confirmation} />

export const Template = (args) => <Confirmation {...args} />;

# Confirmation

<Canvas>
  <Story name="Confirmation" args={{}}>
    {Template.bind({})}
  </Story>
</Canvas>
